[package]
name = "http"
description = "HTTP builtin for project"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[package.metadata.carrier]
kind = "full"
entry = "boot"

[dependencies]
